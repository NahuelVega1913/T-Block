<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.t_block">

    <!-- ========== PERMISOS NECESARIOS ========== -->

    <!-- Mostrar overlays/ventanas flotantes -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Consultar paquetes instalados -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Estadísticas de uso de apps -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Obtener lista de tareas en primer plano -->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <!-- Apagar pantalla -->
    <uses-permission android:name="android.permission.DEVICE_POWER" />

    <!-- Recibir eventos de inicio del sistema -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Acceso a configuración del dispositivo -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- ========== QUERIES PARA PAQUETES ========== -->
    <queries>
        <!-- Permite consultar apps instaladas -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- ========== APPLICATION ========== -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.TBlock"
        android:usesCleartextTraffic="false">

        <!-- ========== MAIN ACTIVITY ========== -->
        <activity
            android:name="com.example.t_block.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TBlock"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ========== BLOCK OVERLAY ACTIVITY ========== -->
        <activity
            android:name="com.example.t_block.BlockOverlayActivity"
            android:exported="false"
            android:theme="@style/Theme.TBlock"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:noHistory="true" />

        <!-- ========== PROTECTION ACTIVITY ========== -->
        <activity
            android:name="com.example.t_block.ProteccionActivity"
            android:exported="false"
            android:theme="@style/Theme.TBlock"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:noHistory="true" />

        <!-- ========== ACCESSIBILITY SERVICE ========== -->
        <service
            android:name="com.example.t_block.AppMonitorService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ========== DEVICE ADMIN RECEIVER ========== -->
        <receiver
            android:name="com.example.t_block.MyDeviceAdminReceiver"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true"
            android:enabled="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLE_REQUESTED" />
            </intent-filter>
        </receiver>

        <!-- ========== PACKAGE INSTALLATION RECEIVER ========== -->
        <receiver
            android:name="com.example.t_block.BlockNewAppsReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- ========== BOOT COMPLETED RECEIVER ========== -->
        <receiver
            android:name="com.example.t_block.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>